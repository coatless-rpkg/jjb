% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autograde.R
\name{gen_agfile}
\alias{gen_agfile}
\title{Autograde Classification Format}
\usage{
gen_agfile(yhat, file.name = "ag_sub")
}
\arguments{
\item{yhat}{A \code{vector} of predicted \eqn{y} values.}

\item{file.name}{A \code{string} to name the file (without a file extension).}
}
\value{
A \code{.csv} with observations on each new line written to the working
directory given by \code{getwd()}.
}
\description{
Creates a valid autograde file when supplied a prediction vector
}
\examples{

# Set seed for reproducibility
set.seed(1337)

# Simulate values
n = 100
beta = 2.5
x = beta*rnorm(n, 0, 1)
y = x + rnorm(n, 1, 2)

# Create a regression model
mod = lm(y~x)

## Predict under model
yhat = predict(mod)

gen_agfile(yhat,file.name="ag_org")

## Predict under new data
new = data.frame(x = seq(-3, 3, 0.5))
yhat2 = predict(mod, new)

gen_agfile(yhat2, file.name="ag_new")
}

